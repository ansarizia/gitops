apiVersion: v1
kind: Service
metadata:
  name: {{ include "myhelmproject.fullname" . }}
  labels:
    {{- include "myhelmproject.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myhelmproject.selectorLabels" . | nindent 4 }}
